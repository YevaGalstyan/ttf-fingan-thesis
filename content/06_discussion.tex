\section{Discussion}\label{sec:discussion}
This section discusses the findings of the study and the limitations of the methodological approach. It covers the sources of the deviation between the generated option prices and the market prices. The questions the study could not answer are discussed next. The section closes with directions for further work. The study evaluated whether the price paths generated by a GAN reproduce the implied volatility of the market. They do not. The generated prices deviate further from the market than the prices of GBM and of both bootstrap modes in every moneyness range. In addition, the deviation is a systematic underpricing.

\subsection{Sources of the Deviation}\label{sec:deviation-sources}

Several factors contribute to the deviation between the generated option prices and the market prices. These include the spread of the generated distribution, the size of the training sample, the returns used as the condition and the recursive rollout of Sec.~\ref{sec:pricing-setup}.

The generated distribution is narrower than the returns of the test split. The standard deviation of the generated returns is \num{0.0369} against \num{0.0404} for the test split, and the excess kurtosis \num{10.12} against \num{13.27} (Sec.~\ref{sec:network-capacity}). As a result, the option prices are underestimated across all moneyness ranges.
The underpricing is visible not only in the tails but also at the at-the-money strikes. $G$ has an implied volatility error of \num{0.243} for at-the-money strikes, compared with \num{0.144} of the block bootstrap. The prices at the at-the-money strikes do not depend on the extreme returns, so the deviation is not caused by the tail weight alone. 

The size of the training sample limits the tail weight of the generated returns. The training split contains \num{3197} returns (Sec.~\ref{sec:data-split}). \textcite{fuTimeSeriesSimulation2019} report that a cGAN underestimated the excess kurtosis of a simulated series at a training sample of \num{1000} observations and matched it at \num{20000}. The excess kurtosis of the generated returns is below the test split, which is consistent with that finding. The available TTF futures data covers the period from \num{2010} (Sec.~\ref{sec:futures-data}), so a longer training sample would require additional data.

The condition variable used in the GAN is the $l$ past returns, so $G$ has to infer the volatility from them. As seen in Fig.~\ref{fig:conditioning}, $G$ responds to the conditioning. The spread of the generated return correlates with the spread of the condition window at \num{0.752}. In contrast, the spread of the bootstrap stays almost constant across the \num{20} valuation dates. However, the spread of $G$ stays below the implied volatility of the market on \num{17} of the \num{20} valuation dates. \textcite{fuTimeSeriesSimulation2019} propose adding the LSTM cell to the generator so that the historical volatility is extracted from the condition automatically. The established architecture of ForGAN contains an LSTM cell as described in Sec.~\ref{sec:forgan}. The response of $G$ to the condition window shows that the volatility is extracted. A longer condition window does not improve the response. Increasing $l$ from \num{10} to \num{30} reduces the tail weight of the generated returns (Sec.~\ref{sec:condition-window}). 

The generated returns are produced recursively (Sec.~\ref{sec:pricing-setup}). After $l$ steps, the condition window holds only the generated returns. At this point, $G$ operates outside the regime it was trained on. The seed spread of the inverted far out-of-the-money strikes is \num{43.3} against a mean of \num{53.8} (Tab.~\ref{tab:price-error-generator}). The seeds disagree about whether the paths reach the outer strikes at all. The seed spread reported in Sec.~\ref{sec:network-capacity} is measured one step ahead and does not reflect the spread after $h$ recursive steps. 

\subsection{Limitations}\label{sec:limitations}

The results are subject to several limitations. These include the model selection criterion, the coverage of the benchmark set, the exclusion of the strikes that carry no implied volatility, the composition of the test data split, and the available TTF front-month futures data.


%
% ## PART 2 — WHAT THE STUDY COULD NOT ESTABLISH
%
% ### Model selection on the generated returns
% - [ ] Sec. 5.6 selected on the seed spread of the generated returns,
%       following the network capacity decision
% - [ ] Sec. 5.7: the PnL*a & MSE branch has the narrower seed spread
%       on most of those statistics and the wider one on the option
%       prices. The ranking reverses
% - [ ] CONSEQUENCE: the selection criterion does not predict the
%       quantity the thesis is about
% - [ ] This is the methodological finding of the study. Do not bury it
%
% ### The benchmarks
% - [ ] GARCH with Student-t innovations is the standard econometric
%       alternative. Not included, since fitting reintroduces the
%       specification choices the bootstrap avoids, and symmetric
%       innovations would not add a property the block bootstrap lacks
% - [ ] BUT a GARCH benchmark conditions the simulated volatility on
%       the state at the valuation date, which neither bootstrap does.
%       Since G does condition (Sec. 5.7), whether the generated prices
%       improve on a conditioned benchmark is left open
%       ^ THE SHARPEST GAP IN THE BENCHMARK SET. Say it plainly
% - [ ] Block bootstrap paths are not independent (Sec. 4.4), so the
%       Monte Carlo standard error is too small in that mode
%
% ### The inversion and the moneyness band
% - [ ] Two mechanisms, both in Sec. 4.6: no path reaches the strike,
%       or every path finishes above it
% - [ ] The excluded strikes are those the model failed hardest on, so
%       the reported errors are lower bounds. Holds for GBM as well
% - [ ] Raising M from 20000 to 50000 changed the total by about two
%       percentage points, so this is structural rather than a Monte
%       Carlo resolution problem
% - [ ] A narrower band would raise the inversion rate at the cost of
%       discarding the strikes where the models differ most
%
% ### The test period
% - [ ] The stylized facts of the test split originate in 57 of its 401
%       trading days (Sec. 5.1). Excluding the 2026 disruption, the
%       excess kurtosis falls from 13.24 to 1.57 and the skewness from
%       0.91 to -0.06
% - [ ] CONSEQUENCE: the distributional comparison of Ch. 5 and the
%       pricing comparison of Sec. 5.7 rest on a narrow base. A
%       different test period would give different magnitudes
% - [ ] Two of the 20 valuation dates fall in or just after that
%       window, and those are the dates where every model drawing on
%       history failed worst
%
% ### The autocorrelation comparison
% - [ ] Neither short split shows a smooth decay (Sec. 5.1), so the
%       test split gives no decay shape to compare against. The decay
%       claim rests on the stylized fact of Sec. 2.6 rather than on the
%       reference column
% - [ ] The direction of the error on the option prices cannot be read
%       off the C_2(tau) table. Volatility clustering slows the
%       convergence of the compounded return distribution to a normal
%       one, so it controls how fast the tails thin over the life of
%       the option. Whether the multi-day tails end up too heavy or too
%       thin depends on the rollout rather than on the daily statistics
%
% ### The data
% - [ ] About 257 missing business days in the front-month series,
%       documented and retained (Sec. 4.1)
% - [ ] The BBO schema has usable two-sided quotes only from 2025, so
%       settlement prices were used throughout
% - [ ] A two-return construction from opening and settlement prices
%       was possible but not pursued: the returns would need
%       aggregating back to daily for the Monte Carlo, and would mix a
%       traded price with an exchange mark
%
% ### Mode collapse
% - [ ] Each condition appears once in the data with a single realized
%       target, so the empirical conditional distribution is a point
%       mass. \textcite{vuleticFinGANForecastingClassifying2024} note
%       that forecasts close to the target therefore receive high
%       scores from the discriminator, which encourages mode collapse
%       in a classical GAN setup
%       ^ DECIDE: is this a limitation or does it belong in Ch. 3?
%
% \textbf{Cost functions.} The two cost functions with economics-driven terms priced in Sec.~\ref{sec:option-prices} deviate further from the market than the ForGAN baseline and the benchmark models.
%
% ## PART 3 — WHAT WOULD HAVE TO CHANGE
%
% ### Condition on volatility, not only on returns
% - [ ] Append a realized volatility measure to the condition vector,
%       for example the rolling standard deviation of the last l
%       returns
% - [ ] Structurally identical to appending any other feature, so the
%       implementation cost is low
% - [ ] Both outcomes informative: clustering appears and the mechanism
%       is demonstrated rather than asserted; it does not and the
%       limitation is architectural
% - [ ] Multi-seed, mean and seed spread. Single runs are not
%       defensible — that is the lesson of Sec. 5.7
%
% ### A conditioned benchmark
% - [ ] GARCH-t, to test whether the conditioning of G improves on a
%       parametric model that also conditions on the state
%
% ### Selection on the target quantity
% - [ ] Selecting on the option prices rather than on the generated
%       returns. Costly, since every candidate must be priced, but the
%       ranking reversal of Sec. 5.7 suggests the one-step statistics
%       are not a sufficient proxy
%
% A longer training sample would require either additional history or a higher sampling frequency. The second would change the series the model is trained on, since the properties of intraday returns differ from those of daily returns.

% Lengthening the condition window does not improve the inference. Increasing 
% 𝑙
% l from \num{10} to \num{30} reduces the tail weight of the generated returns (Sec.~\ref{sec:network-capacity}).
%
% ### Closing — 2 sentences
% - [ ] What the study establishes, stated narrowly
% - [ ] Do not end on an apology. State the boundary of the result
%
%
% ### CHECK BEFORE WRITING
% - [ ] The citation key for Fu et al. — fu2019time
% - [ ] The section number where ForGAN and the LSTM are described
% - [ ] Whether the mode collapse point is already made in Ch. 3